import React from 'react';

import { Form, PlayingCard, TryunfoHeader } from '../components';

const Home = () => (
  <div>
    <TryunfoHeader />
    <section>
      <div><Form /></div>
      <div><PlayingCard /></div>
    </section>
  </div>
);

export default Home;
